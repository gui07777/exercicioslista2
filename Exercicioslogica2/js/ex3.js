var letra = prompt("Letra do seu sexo:");

if(letra === "F" || letra === "f"){
    alert("F-FEMININO")
}
else if ( letra === "M" || letra === "m"){
    alert("M-MASCULINO")
} else {
    alert("Inválido")
}